import React from "react"
import { PageContainer } from "../../components/page-container"
import { PageHeader } from "../../components/page-header"

const Esea: React.FC = React.memo(() => {
  return (
    <PageContainer>
            <PageHeader title="Under development" />
    </PageContainer>
  )
})

export default Esea